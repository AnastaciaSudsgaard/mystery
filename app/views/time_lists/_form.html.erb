<%= form_with(model: time_list) do |form| %>
  <% if time_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(time_list.errors.count, "error") %> prohibited this time_list from being saved:</h2>

      <ul>
        <% time_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
