<%= form_with(model: [@time_item.time_list, @time_item]) do |f| %>
  <% if @time_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_item.errors.count, "error") %> prohibited this time_item from being saved:</h2>

      <ul>
      <% @time_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :completed %><br />
    <%= f.check_box :completed %>
  </div>
  <div class="field">
    <%= f.label :completed_at %><br />
    <%= f.datetime_select :completed_at %>
  </div>
  <div class="field">
    <%= f.label :time_list %><br />
    <%= f.text_field :time_list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
